---
phase: 05-security-hardening-agpl-gate
plan: 05
type: execute
wave: 3
depends_on:
  - 05-04
files_modified:
  - apps/web/core/components/workspace/sidebar/help-section/root.tsx
  - apps/web/core/components/power-k/config/help-commands.ts
  - apps/web/core/components/common/latest-feature-block.tsx
  - apps/web/core/components/global/product-updates/footer.tsx
  - apps/web/core/components/global/product-updates/fallback.tsx
  - apps/web/app/layout.tsx
  - apps/web/app/(all)/workspace-invitations/page.tsx
  - apps/web/app/(all)/[workspaceSlug]/(settings)/settings/projects/page.tsx
  - apps/web/app/error/prod.tsx
  - apps/web/core/components/estimates/root.tsx
  - apps/admin/app/(all)/(dashboard)/sidebar-help-section.tsx
  - apps/admin/app/(all)/(dashboard)/ai/form.tsx
  - apps/api/plane/settings/openapi.py
  - apps/web/core/components/auth-screens/footer.tsx
autonomous: false
requirements:
  - SEC-03
  - SEC-04

must_haves:
  truths:
    - "No user-visible URL in the web or admin frontend still points to *.plane.so domains"
    - "The auth-screen footer contains a 'Source code (AGPL)' link pointing to /source (satisfying the AGPL §13 offer from Plan 04)"
    - "The help-section sidebar and power-k commands no longer offer Plane docs/forum/GitHub links"
    - "Error page support contact points to the Bright-Byte support email placeholder (not support@plane.so)"
    - "OpenAPI spec title and contact URL are updated to Bright-Byte values"
    - "OG image URLs in layout.tsx no longer reference app.plane.so"
    - "License.txt and COPYRIGHT.txt are byte-identical to pre-plan state"
  artifacts:
    - path: "apps/web/core/components/auth-screens/footer.tsx"
      provides: "AGPL §13 source offer link ('Source code (AGPL)') pointing to /source"
      contains: "/source"
    - path: "apps/web/core/components/workspace/sidebar/help-section/root.tsx"
      provides: "plane.so docs/forum links replaced with Bright-Byte support URL or removed"
      contains: "bright-byte"
    - path: "apps/web/app/error/prod.tsx"
      provides: "support@plane.so and status.plane.so replaced with Bright-Byte equivalents"
      contains: "bright-byte"
    - path: "apps/api/plane/settings/openapi.py"
      provides: "API title/contact updated to Bright-Byte values"
      contains: "Bright-Byte"
  key_links:
    - from: "apps/web/core/components/auth-screens/footer.tsx AGPL link"
      to: "apps/api/plane/web/urls.py path('source', ...)"
      via: "href='/source' in AuthFooter component"
      pattern: "/source"
---

<objective>
Replace all remaining plane.so URLs in frontend components with Bright-Byte equivalents (or remove links where no Bright-Byte equivalent exists), and add the AGPL §13 source offer link to the auth-screen footer.

Purpose: Sixteen plane.so URLs were deferred from Phase 1's brand sweep to Phase 5 (logged in 01-06-BRAND-SWEEP.md). These must be cleaned up before client cutover — they would expose clients to Plane's infrastructure (docs, forum, changelog, support) and the help-section links are user-visible. The auth-footer AGPL link ties together the /source endpoint created in Plan 04, completing the AGPL §13 offer.
Output: All 16 plane.so frontend URLs neutralized; AGPL link in auth footer; OG image URLs updated; openapi.py Bright-Byte branded; checkpoint for real Bright-Byte URL decisions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-security-hardening-agpl-gate/05-RESEARCH.md
@.planning/phases/01-user-facing-rebrand/01-06-BRAND-SWEEP.md

<interfaces>
<!-- DEFERRED FRONTEND URLs (from 01-06-BRAND-SWEEP.md Section 11, OUT-OF-PHASE rows O1-O17 + Section 8 + layout.tsx): -->

<!-- apps/web/core/components/workspace/sidebar/help-section/root.tsx -->
<!--   :52 "https://go.plane.so/p-docs" — replace with "mailto:support@bright-byte.example" or remove -->
<!--   :83 "https://forum.plane.so" — replace with "mailto:support@bright-byte.example" or remove -->

<!-- apps/web/core/components/power-k/config/help-commands.ts -->
<!--   :40 "https://docs.plane.so/" — replace/remove help command entry -->
<!--   :53 "https://forum.plane.so" — replace/remove -->
<!--   :66 "https://github.com/makeplane/plane/issues/new/choose" — replace/remove -->

<!-- apps/web/core/components/common/latest-feature-block.tsx -->
<!--   :23 "https://plane.so/changelog" — replace or remove component entirely (it fetches Plane changelog) -->
<!--   :36 alt="Plane Work items" — update alt text to "Bright-Byte PMP Work items" -->

<!-- apps/web/core/components/global/product-updates/footer.tsx -->
<!--   :21 "https://go.plane.so/p-docs" — remove/replace -->
<!--   :33 "https://go.plane.so/p-changelog" — remove/replace -->
<!--   :44 "mailto:support@plane.so" — replace with "mailto:support@bright-byte.example" -->
<!--   :55 "https://forum.plane.so" — remove/replace -->
<!--   :64 "https://plane.so/pages" — remove/replace -->
<!--   Note: :73 {t("powered_by_plane_pages")} is i18n key — Phase 2 scope, do NOT touch -->

<!-- apps/web/core/components/global/product-updates/fallback.tsx -->
<!--   :19 "https://plane.so/changelog?category=cloud" — remove/replace -->
<!--   :20 "https://plane.so/changelog?category=self-hosted" — remove/replace -->

<!-- apps/web/app/layout.tsx -->
<!--   :45 { property: "og:url", content: "https://app.plane.so/" } — replace with VITE_WEB_URL env or "#" -->
<!--   :46 { property: "og:image", content: "https://app.plane.so/og-image.png" } — replace with self-hosted OG image or "#" -->
<!--   :52 { name: "twitter:image", content: "https://app.plane.so/og-image.png" } — same -->

<!-- apps/web/app/(all)/workspace-invitations/page.tsx -->
<!--   :118 "https://forum.plane.so" — replace with "mailto:support@bright-byte.example" or remove -->

<!-- apps/web/app/(all)/[workspaceSlug]/(settings)/settings/projects/page.tsx -->
<!--   :35 "https://plane.so/" — replace with "/" (home) or remove link -->

<!-- apps/web/app/error/prod.tsx -->
<!--   :20 "mailto:support@plane.so" — replace with "mailto:support@bright-byte.example" -->
<!--   :25 "https://status.plane.so/" — replace with "#" (no Bright-Byte status page yet) -->

<!-- apps/web/core/components/estimates/root.tsx -->
<!--   :114 "https://docs.plane.so/core-concepts/projects/run-project#estimate" — replace with "#" or remove link -->

<!-- apps/admin/app/(all)/(dashboard)/sidebar-help-section.tsx -->
<!--   :24 "https://docs.plane.so/" — replace/remove -->
<!--   :29 "https://forum.plane.so" — replace/remove -->
<!--   :34 "https://github.com/makeplane/plane/issues/new/choose" — replace/remove -->

<!-- apps/admin/app/(all)/(dashboard)/ai/form.tsx -->
<!--   :132 "https://plane.so/contact" — remove link or replace with "mailto:support@bright-byte.example" -->

<!-- apps/admin/components/instance/setup-form.tsx -->
<!--   :358 "https://developers.plane.so/self-hosting/telemetry" — ALREADY handled in Plan 03 (changed to href="#") — do NOT touch here -->

<!-- apps/api/plane/settings/openapi.py -->
<!--   :15-24 Plane API title, contact name/email/URL — update to Bright-Byte values -->
<!--   :48 "https://api.plane.so" server URL — update to Bright-Byte API URL or remove -->
<!--   Note: openapi.py only active when ENABLE_DRF_SPECTACULAR=1 env var is set -->

<!-- apps/web/core/components/auth-screens/footer.tsx — AGPL §13 offer link: -->
<!-- Current content (45 lines): shows brand logos, "Join 10,000+ teams" text -->
<!-- Add a subtle footer link: "Source code (AGPL)" pointing to "/source" -->
<!-- Place it below the existing content, in a new <div> or appended to the existing <div> -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Real Bright-Byte support/docs/forum URLs for the 16 frontend link replacements</decision>
  <context>
    The 16 plane.so frontend URLs need replacement or removal. The plan defaults are:
    - Docs links: href="#" (remove functionality; docs do not exist yet)
    - Forum links: href="#" or remove (no Bright-Byte forum)
    - Support email links: "mailto:support@bright-byte.example" (same placeholder used in Phase 1)
    - Changelog links: remove (no Bright-Byte changelog)
    - Bug report (GitHub): href="#" or remove (internal; clients should use Bright-Byte support)
    - Status page: href="#" (no Bright-Byte status page yet)
    - Marketing/pages URLs: remove
    - OG image URLs: point to "/" or keep empty (no self-hosted OG image yet)

    If Bright-Byte has real URLs for any of these (support email, docs, status page), they can be used instead of the placeholders.

  </context>
  <options>
    <option id="option-a-placeholders">
      <name>Use placeholders / remove links (default)</name>
      <pros>Unblocks execution immediately; no dead Plane links in production</pros>
      <cons>Some UI affordances (help, docs) temporarily non-functional; acceptable for v1</cons>
    </option>
    <option id="option-b-real-urls">
      <name>Provide real Bright-Byte URLs for some or all replacements</name>
      <pros>Better UX for clients</pros>
      <cons>Requires user to have set up support/docs infrastructure first</cons>
    </option>
  </options>
  <resume-signal>Select: option-a-placeholders (proceed with defaults above) OR option-b-real-urls (provide the real URLs you want used: support email, docs URL, status URL, etc.). For each URL you want customized, supply the value — any not supplied will use the default placeholder.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Replace all 16 plane.so frontend URLs and add AGPL footer link</name>
  <files>
    apps/web/core/components/workspace/sidebar/help-section/root.tsx,
    apps/web/core/components/power-k/config/help-commands.ts,
    apps/web/core/components/common/latest-feature-block.tsx,
    apps/web/core/components/global/product-updates/footer.tsx,
    apps/web/core/components/global/product-updates/fallback.tsx,
    apps/web/app/layout.tsx,
    apps/web/app/(all)/workspace-invitations/page.tsx,
    apps/web/app/(all)/[workspaceSlug]/(settings)/settings/projects/page.tsx,
    apps/web/app/error/prod.tsx,
    apps/web/core/components/estimates/root.tsx,
    apps/admin/app/(all)/(dashboard)/sidebar-help-section.tsx,
    apps/admin/app/(all)/(dashboard)/ai/form.tsx,
    apps/api/plane/settings/openapi.py,
    apps/web/core/components/auth-screens/footer.tsx
  </files>
  <read_first>
    - apps/web/core/components/workspace/sidebar/help-section/root.tsx (full file — lines 45-90 for the two plane.so URLs)
    - apps/web/core/components/power-k/config/help-commands.ts (lines 35-70 for the three command URL entries)
    - apps/web/core/components/common/latest-feature-block.tsx (lines 18-40 for changelog URL and alt text)
    - apps/web/core/components/global/product-updates/footer.tsx (lines 15-80 for the five URLs; note line 73 is i18n key — DO NOT TOUCH)
    - apps/web/core/components/global/product-updates/fallback.tsx (lines 15-25 for two changelog URLs)
    - apps/web/app/layout.tsx (lines 40-55 for OG/twitter meta entries)
    - apps/web/app/(all)/workspace-invitations/page.tsx (lines 113-125 for forum URL)
    - apps/web/app/(all)/[workspaceSlug]/(settings)/settings/projects/page.tsx (lines 30-40 for plane.so link)
    - apps/web/app/error/prod.tsx (lines 15-30 for mailto and status URL)
    - apps/web/core/components/estimates/root.tsx (lines 110-120 for docs URL)
    - apps/admin/app/(all)/(dashboard)/sidebar-help-section.tsx (lines 20-40 for three URLs)
    - apps/admin/app/(all)/(dashboard)/ai/form.tsx (lines 128-138 for contact URL)
    - apps/api/plane/settings/openapi.py (lines 12-55 for title/contact/server block)
    - apps/web/core/components/auth-screens/footer.tsx (full file — 45 lines; understand where to add AGPL link)
  </read_first>
  <action>
    Apply the replacements decided in the checkpoint (default: placeholders/remove; or URLs from option-b).

    For each file, apply ONLY the URL-related changes at the exact lines identified in the research. Do not make formatting, import, or logic changes beyond the URL strings.

    Specific replacements (using checkpoint:decision defaults — adjust if real URLs supplied):

    help-section/root.tsx — :52 and :83: Replace both plane.so URLs with "mailto:support@bright-byte.example". If the surrounding JSX is an `<a href="...">` element, keep the element structure but change only the href value and visible text label (e.g., "Support" instead of "Documentation" / "Community"). If the elements are complex, it is acceptable to remove the link elements entirely and replace with a plain text span "Support: support@bright-byte.example".

    power-k/config/help-commands.ts — :40, :53, :66: For each command entry with a plane.so URL, either remove the entire command entry from the array (cleanest for v1 — no broken links in the command palette) OR replace the URL with "#" and keep the command. Removing is preferred since there are no Bright-Byte equivalents yet. Ensure the array remains valid TypeScript (no trailing commas after last element; verify with TypeScript compiler).

    latest-feature-block.tsx — :23: Replace "https://plane.so/changelog" URL with "#". :36: Replace alt="Plane Work items" with alt="Bright-Byte PMP Work items".

    product-updates/footer.tsx — :21, :33: Replace plane.so docs/changelog href with "#". :44: Replace "mailto:support@plane.so" with "mailto:support@bright-byte.example". :55: Replace forum URL with "#". :64: Replace "https://plane.so/pages" with "#". Do NOT touch line 73 ({t("powered_by_plane_pages")}) — this is an i18n key render, not a URL, and is Phase 2 scope.

    product-updates/fallback.tsx — :19 and :20: Replace both plane.so changelog URLs with "#".

    layout.tsx — :45 og:url: Replace "https://app.plane.so/" with "/" (relative, no hard-coded domain). :46 og:image: Replace "https://app.plane.so/og-image.png" with "/og-image.png" (use the local asset path — the file is already in apps/web/public/). :52 twitter:image: Same replacement as og:image.

    workspace-invitations/page.tsx — :118: Replace "https://forum.plane.so" with "mailto:support@bright-byte.example".

    projects/page.tsx — :35: Replace "https://plane.so/" with "/" (home route). Update visible link text if it currently reads "plane.so" or similar.

    error/prod.tsx — :20: Replace "mailto:support@plane.so" with "mailto:support@bright-byte.example". :25: Replace "https://status.plane.so/" with "#" (no Bright-Byte status page yet).

    estimates/root.tsx — :114: Replace "https://docs.plane.so/core-concepts/projects/run-project#estimate" with "#".

    admin/sidebar-help-section.tsx — :24, :29, :34: Replace the three plane.so/GitHub URLs. Docs URL → "#". Forum URL → "#". GitHub bug-report URL → "#" or remove the entire "Report a bug" link if removing is cleaner.

    admin/ai/form.tsx — :132: Replace "https://plane.so/contact" with "mailto:support@bright-byte.example".

    openapi.py — Update the SPECTACULAR_SETTINGS dict: change "TITLE" from "The Plane REST API" to "The Bright-Byte PMP REST API". Change CONTACT "name" from "Plane" to "Bright-Byte". Change CONTACT "email" from "support@plane.so" to "support@bright-byte.example". Change CONTACT "url" from "https://plane.so" to "/". Change the SERVERS entry URL from "https://api.plane.so" to "/" (relative, no hard-coded domain). Do NOT add or remove the AGPL header. openapi.py is a Python file — do not edit its AGPL header.

    auth-screens/footer.tsx — AGPL §13 offer: Add a new `<div>` at the bottom of the returned JSX (inside the outer `<div className="flex flex-col items-center gap-6">`, after the brand logos div). The new element should be a small `<p>` or `<span>` with className for subtle tertiary text (matching the existing "Join 10,000+ teams" span style), containing an `<a href="/source">` anchor with text "Source code (AGPL)" and `target="_blank" rel="noopener noreferrer"`. This satisfies the AGPL §13 requirement that the offer be "prominent" and accessible to users of the network service. The footer appears on the login screen — accessible before authentication.

    TypeScript/lint compliance: All modified TSX/TS files must still pass `pnpm check:types`. Removing array entries from help-commands.ts must leave valid syntax. No new imports required for any of these URL-only changes.

  </action>
  <verify>
    <automated>grep -rn "plane\.so\|app\.plane\.so" /Users/Hussi/Desktop/Bright-Byte-PMP/apps/web/core/components/workspace/sidebar/help-section/root.tsx /Users/Hussi/Desktop/Bright-Byte-PMP/apps/web/core/components/power-k/config/help-commands.ts /Users/Hussi/Desktop/Bright-Byte-PMP/apps/web/core/components/common/latest-feature-block.tsx /Users/Hussi/Desktop/Bright-Byte-PMP/apps/web/core/components/global/product-updates/footer.tsx /Users/Hussi/Desktop/Bright-Byte-PMP/apps/web/core/components/global/product-updates/fallback.tsx /Users/Hussi/Desktop/Bright-Byte-PMP/apps/web/app/layout.tsx /Users/Hussi/Desktop/Bright-Byte-PMP/apps/web/app/error/prod.tsx /Users/Hussi/Desktop/Bright-Byte-PMP/apps/admin/app/\(all\)/\(dashboard\)/sidebar-help-section.tsx /Users/Hussi/Desktop/Bright-Byte-PMP/apps/admin/app/\(all\)/\(dashboard\)/ai/form.tsx | grep -v "^[^:]*:#\|// \|/\*" | wc -l</automated>
  </verify>
  <done>The grep returns 0 (no remaining plane.so URLs in the modified files, excluding comments). auth-screens/footer.tsx contains "/source" link. AGPL headers on all modified .tsx/.ts files are unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 16 plane.so frontend URLs neutralized across web and admin apps. AGPL §13 source offer link added to the auth-screen footer. Admin telemetry form already handled in Plan 03. OpenAPI spec updated.
  </what-built>
  <how-to-verify>
    Automated grep verification was done in Task 2. These visual checks confirm the experience is clean for clients:

    1. Open the web app at localhost (or the Render preview URL). On the login screen, scroll to the bottom of the auth form — verify "Source code (AGPL)" link appears and, when clicked, opens /source returning JSON with "license": "AGPL-3.0-only".

    2. Log in. Open the sidebar help section (? icon or similar). Confirm: no "Plane Documentation", "Forum", or Plane GitHub bug-report links appear. If links were removed, verify no broken UI elements (blank space, console errors).

    3. Open the command palette (Cmd+K). Type "docs" or "forum". Confirm no Plane docs/forum/bug-report commands appear.

    4. Visit /workspace-invitations — confirm the forum.plane.so link is gone or replaced with a Bright-Byte email.

    5. Open the admin panel. Check the sidebar help section — confirm no Plane docs/forum/GitHub links. Check the AI settings page — confirm no "plane.so/contact" link.

    6. Run the phase-wide print sweep: `grep -rn "print(" apps/api/plane/ --include="*.py" | grep -v "/migrations/" | grep -v "settings.DEBUG" | grep -v "# " | wc -l` — confirm it returns 0 or a low number consisting only of known safe prints (e.g., management commands, tests).

    7. AGPL integrity: `git diff --quiet LICENSE.txt COPYRIGHT.txt` — exits 0 (no modification to license files).

  </how-to-verify>
  <resume-signal>Type "approved" if all visual checks pass, or describe any issues found (which file, which element, what you see).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                                                         |
| ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- |
| Frontend → Plane infrastructure      | Every plane.so URL in the frontend is a live link to Plane's servers; after client cutover, these expose clients to Plane's domains |
| User browser → auth footer AGPL link | The /source link must be accessible from the app interface to satisfy AGPL §13's "prominent" requirement                            |
| admin UI → external docs links       | Admin links to developers.plane.so or plane.so still active would route admins to Plane's infrastructure                            |

## STRIDE Threat Register

| Threat ID | Category                 | Component                                                                          | Disposition | Mitigation Plan                                                                                                             |
| --------- | ------------------------ | ---------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------- |
| T-5-05a   | Information Disclosure   | Frontend links to \*.plane.so expose client data/browsing to Plane's server logs   | mitigate    | Replace all 16 URLs with Bright-Byte equivalents or "#"; verified by grep gate                                              |
| T-5-05b   | Repudiation / Compliance | AGPL §13 offer not "prominent" in UI — /source endpoint exists but no visible link | mitigate    | Add "Source code (AGPL)" link to auth-screen footer (visible to unauthenticated users); depends on Plan 04 /source endpoint |
| T-5-05c   | Tampering                | Editing TSX files risks accidental header modification (AGPL addlicense failure)   | mitigate    | Only URL string values are changed; AGPL header lines 1-4 of each file must be verified unchanged                           |
| T-5-05d   | Tampering                | COPYRIGHT.txt or LICENSE.txt accidentally modified                                 | mitigate    | Acceptance check: `git diff --quiet LICENSE.txt COPYRIGHT.txt` exits 0 before plan sign-off                                 |
| T-5-SC    | Tampering                | No npm/pip installs in this plan                                                   | accept      | URL-only changes; no new packages                                                                                           |

</threat_model>

<verification>
Phase gate checks for this plan:

1. No plane.so URLs in modified files:
   `grep -rn "plane\.so" apps/web/core/components/workspace/sidebar/help-section/root.tsx apps/web/core/components/power-k/config/help-commands.ts apps/web/core/components/common/latest-feature-block.tsx apps/web/core/components/global/product-updates/ apps/web/app/layout.tsx apps/web/app/error/prod.tsx apps/web/core/components/estimates/root.tsx apps/admin/app/\(all\)/\(dashboard\)/sidebar-help-section.tsx apps/admin/app/\(all\)/\(dashboard\)/ai/form.tsx | grep -v "^[^:]*:#\|// \|/\*" | wc -l` returns 0

2. No app.plane.so in layout.tsx:
   `grep -c "app\.plane\.so" apps/web/app/layout.tsx` returns 0

3. AGPL offer in footer:
   `grep -c "/source" apps/web/core/components/auth-screens/footer.tsx` returns at least 1

4. Workspace-invitations page clean:
   `grep -c "forum\.plane\.so" "apps/web/app/(all)/workspace-invitations/page.tsx"` returns 0

5. projects/page.tsx clean:
   `grep -c "plane\.so" "apps/web/app/(all)/[workspaceSlug]/(settings)/settings/projects/page.tsx"` returns 0

6. openapi.py updated:
   `grep -c "Bright-Byte" apps/api/plane/settings/openapi.py` returns at least 2

7. TypeScript passes: `pnpm --filter web check:types` exits 0

8. AGPL integrity: `git diff --quiet LICENSE.txt COPYRIGHT.txt` exits 0

9. Human-verify checkpoint approved
   </verification>

<success_criteria>

- All 16 plane.so frontend URLs from the Phase 1 brand sweep OUT-OF-PHASE list are replaced or removed in web and admin apps
- The auth-screen footer has a visible "Source code (AGPL)" link pointing to /source
- Error page support email points to support@bright-byte.example (not plane.so)
- OpenAPI spec title/contact/server reflects Bright-Byte branding
- TypeScript compilation succeeds for all modified files
- Human-verify checkpoint confirms no broken UI, no console errors, /source endpoint reachable from footer link
- LICENSE.txt and COPYRIGHT.txt unchanged (AGPL integrity gate passes)
  </success_criteria>

<output>
Create `.planning/phases/05-security-hardening-agpl-gate/05-05-SUMMARY.md` when done
</output>
