# Deferred Items — Phase 01 (user-facing-rebrand)

Out-of-scope discoveries logged during execution. NOT fixed by the discovering plan.

| Discovered In    | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Why Deferred                                                                                                                                                                                             | Status                                                                                                      |
| ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| 01-04 (Task 1/3) | `pnpm --filter @plane/propel run check:types` reports 34 pre-existing `TS2307 Cannot find module '@plane/constants'/'@plane/types'` errors plus cascading implicit-`any` errors in unrelated chart/state-icon files (radar-chart, scatter-chart, tree-map, intake-state-group-icon). Root cause: `packages/constants/dist` and `packages/types/dist` are not built in this environment (turbo `^build` ordering not run). The `pi-chat.tsx` edit itself produces ZERO type errors. | Pre-existing; not caused by the AI/icon/i18n rebrand. SCOPE BOUNDARY rule: only auto-fix issues directly caused by the current task. Requires a full `pnpm build` of workspace packages, not a code fix. | Open — flag for Phase 2 (atomic `@plane/*` rename lands with a green full build) or a build-env setup pass. |
